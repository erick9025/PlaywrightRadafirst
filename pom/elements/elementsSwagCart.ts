import { ElementsSwag } from "./elementsSwag";

export class ElementsSwagCart implements ElementsSwag {
    //Define locators that should return LIST OF elements
    public readonly IconCart: string = "a.shopping_cart_link";
    public readonly PageTitle: string = ".title";  
}