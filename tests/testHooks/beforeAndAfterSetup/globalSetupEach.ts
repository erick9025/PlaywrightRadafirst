//test.beforeEach(
async function globalSetupEach() {
    console.log("Global setup each executed");
    // You can perform any global setup tasks here, such as initializing databases, setting environment variables, etc.
}

export default globalSetupEach;